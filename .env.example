# Environment Variables for Weight & Balance Calculator

# PostgreSQL Database Configuration (for Vercel deployment)
POSTGRES_URL="postgres://username:password@hostname:port/database?sslmode=require"
POSTGRES_PRISMA_URL="postgres://username:password@hostname:port/database?sslmode=require&pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://username:password@hostname:port/database?sslmode=require"
POSTGRES_USER="your_username"
POSTGRES_HOST="your_host"
POSTGRES_PASSWORD="your_password"
POSTGRES_DATABASE="your_database"

# Application Configuration
NODE_ENV="production"
VERCEL_ENV="production"

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. For Vercel deployment, add these variables to your Vercel project settings
# 3. For local development, you can use a local PostgreSQL instance or continue using localStorage
# 4. The app will gracefully fallback to localStorage if database connection fails